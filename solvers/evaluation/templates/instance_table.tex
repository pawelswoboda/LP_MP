\documentclass{standalone}
\usepackage{booktabs}
\begin{document}

\begin{table}
{% set solvers =  c.execute("SELECT id,algorithmName FROM Solvers ORDER BY id").fetchall() -%}
{% set no_algorithms = solvers|length -%}
\begin{tabular}{l{{ "c" * (solvers|length) }}}
\hline

Instance 
{% for a in solvers -%}
& {{ a[1] }}
{% endfor-%}
\\ \hline

{% for dataset_row in c.execute("SELECT id,name FROM Datasets;") %}
   \multicolumn{ {{ 1 + no_algorithms }} }{ c }{ {{ dataset_row[1] }} } \\ \hline
   {% for instance_row in conn.cursor().execute("SELECT id,name FROM Instances WHERE dataset_id = '" + dataset_row[0]|string + "';") %}
      \multirow{3}{*}{ {{ instance_row[1] }} }
      {% for solver_row in conn.cursor().execute("SELECT lowerBound, solver_id FROM LowerBoundView WHERE instance_id = '" + instance_row[0]|string + "' ORDER BY solver_id;") %}
         & {{ solver_row[0] }} 
      {% endfor %}
      \\ 
      {% for solver_row in conn.cursor().execute("SELECT upperBound, solver_id FROM UpperBoundView WHERE instance_id = '" + instance_row[0]|string + "' ORDER BY solver_id;") %}
         & {{ solver_row[0] }} 
      {% endfor %}
      \\ 
      {% for solver_row in conn.cursor().execute("SELECT runtime, solver_id FROM RuntimeView WHERE instance_id = '" + instance_row[0]|string + "' ORDER BY solver_id;") %}
         & {{ solver_row[0]/1000 }} 
      {% endfor %}
      \\ \hline
   {% endfor %}
{% endfor %}

\end{tabular}
\end{table}

\end{document}
 
